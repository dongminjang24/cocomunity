spring:
  profiles:
    active: test
  datasource:
    url: jdbc:h2:mem:com.cocomunity
    username: sa
    password:
    driver-class-name: org.h2.Driver
  h2:
    console:
      enabled: true
      path: /h2-console
#jdbcTemplate sql log

#MyBatis

